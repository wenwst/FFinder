import SwiftUI

struct HeaderView: View {
    var body: some View {
        Text("File Search")
            .font(.system(size: 32, weight: .bold, design: .rounded))
            .padding()
            .foregroundColor(.primary)
    }
}
